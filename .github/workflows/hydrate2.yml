name: P1 transfer retired
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Corrupt P1 chunk hydration is retired during rebaseline; artifact cleanup follows a green replacement baseline."
